services:
  ppa_packages:
    build:
      context: .
      dockerfile: "${DOCKERFILE:-Dockerfile}"
    ports:
      - 80:80
    restart: always
    volumes:
      - ${VOLUMES_ROOT}/ppa_packages:/var/www/html/debian
